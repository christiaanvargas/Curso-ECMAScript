// función de tipo standard

function square(num) {
    return num * num;
}

// función de flecha (Arror funtion)
const square = (num) => {
    return num * num;
}

// funtion fecha con return implicito
const square = num => num * num;